---
import { PRICES } from "@consts";
import Container from "@components/Container.astro";
---

<Container size="xl">
  <section class="py-16 px-4">
    <div class="max-w-6xl mx-auto">
      <div class="text-center mb-12">
        <h2 class="text-3xl font-bold tracking-tight mb-4 text-black">
          Inversión en tu futuro deportivo
        </h2>
        <p class="text-muted-foreground max-w-2xl mx-auto">
          Únete a nuestra academia y comienza tu camino hacia la excelencia
          deportiva con planes diseñados para tu éxito.
        </p>
      </div>

      <div class="grid md:grid-cols-2 gap-8 items-center">
        <div class="space-y-6">
          {
            PRICES.map((item) => (
              <div class="rounded-xl border bg-card text-card-foreground shadow p-6 transition-all hover:shadow-lg">
                <div class="flex items-start gap-4">
                  <div>
                    <h3 class="font-semibold text-xl mb-2 flex items-center justify-between">
                      {item.name}
                      <span class="text-primary">S/.{item.price}</span>
                    </h3>
                    <p class="text-muted-foreground text-sm">
                      {item.description}
                    </p>
                  </div>
                </div>
              </div>
            ))
          }
        </div>

        <div class="relative">
          <img
            src="https://images.unsplash.com/photo-1526232761682-d26e03ac148e?auto=format&fit=crop&q=80&w=800"
            alt="Promoción especial"
            class="rounded-lg shadow-xl w-full object-cover aspect-[4/3]"
          />
        </div>
      </div>
    </div>
  </section>
</Container>
