---
import { TRANSPORT, LANDMARK, CONTACT } from "@consts";
import Container from "@components/Container.astro";

const mapUrl ="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d3899.796440385665!2d-77.00415352519973!3d-12.194249787396094!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x9105b9c7899fa387%3A0xce1fe9a410a5bd2e!2sCantolao%20Chorrillos!5e0!3m2!1ses-419!2spe!4v1735148864059!5m2!1ses-419!2spe";
---

<Container size="xl">
  <section class="py-8 relative" aria-labelledby="location-title">
    <div class="text-center mb-12">
      <span class="text-accent-dark dark:text-accent-light text-sm font-semibold tracking-wider uppercase mb-2 block transition-colors">Encuéntranos aquí</span>
      <h2 id="location-title" class="text-4xl md:text-5xl font-bold mb-4 bg-gradient-to-r from-accent-dark to-accent-light bg-clip-text">Nuestra Ubicación</h2>
      <p class="text-gray-600 dark:text-gray-400 text-lg max-w-2xl mx-auto leading-relaxed"> Nos encontramos en Chorrillos, dentro del Cuartel Tarapacá en donde se encuentran nuestras instalaciones.</p>
    </div>

    <div class="grid lg:grid-cols-5 md:grid-cols-2 gap-8 mb-12"> 
      <div class="lg:col-span-3 h-[500px] rounded-xl overflow-hidden shadow-lg transition-shadow hover:shadow-xl">
        <iframe
          src={mapUrl}
          class="w-full h-full border-0"
          loading="lazy"
          title="Mapa de ubicación de Cantolao Chorrillos"
          referrerpolicy="no-referrer-when-downgrade"
          aria-label="Mapa interactivo mostrando la ubicación de Cantolao Chorrillos"
        >
        </iframe>
      </div>

      <div class="lg:col-span-2 flex flex-col gap-6">
        <div class="bg-white dark:bg-black/40 dark:backdrop-blur-md rounded-xl p-6 shadow-lg transition-all duration-300 dark:border dark:border-white/30">
          <h3 class="text-xl font-bold mb-4 text-gray-darkest dark:text-white">Dirección Principal</h3>
          <p class="text-gray-600 dark:text-gray-300 mb-4">{CONTACT.address.street}<br />
            {CONTACT.address.district}, {CONTACT.address.city}
            {CONTACT.address.postalCode}
          </p>
          <div class="grid grid-cols-2 gap-4 text-sm">
            <div>
              <span class="block text-gray-500 dark:text-gray-400">Distrito</span>
              <span class="font-medium text-gray-900 dark:text-white">Chorrillos</span>
            </div>
            <div>
              <span class="block text-gray-500 dark:text-gray-400">Referencia</span>
              <span class="font-medium text-gray-900 dark:text-white">Cuartel Tarapacá</span>
            </div>
          </div>
        </div>

        <div class="bg-white dark:bg-black/40 dark:backdrop-blur-md rounded-xl p-6 shadow-lg transition-all duration-300 dark:border dark:border-white/30">
          <h3 class="text-xl font-bold mb-4 text-gray-darkest dark:text-white">Horario de Atención</h3>
          <div class="space-y-3">
            {
              CONTACT.schedule.map(({ day, hours }) => (
                <div class="flex justify-between">
                  <span class="text-gray-600 dark:text-gray-300">{day}</span>
                  <span class="font-medium text-gray-900 dark:text-white">
                    {hours}
                  </span>
                </div>
              ))
            }
          </div>
        </div>
      </div>
    </div>

    <div class="grid md:grid-cols-2 gap-8 bg-white dark:bg-black/40 dark:backdrop-blur-md rounded-xl shadow-lg p-6 dark:border dark:border-white/30">
      <div>
        <h3 class="text-xl font-bold mb-6 flex items-center gap-2 text-gray-darkest dark:text-white">
          <svg class="w-5 h-5 stroke-current">
            <use href="/ui.svg#location"></use>
          </svg>Puntos de Referencia
        </h3>
        <div class="space-y-4">
          {
            LANDMARK.map((landmark) => (
              <div class="flex items-start gap-4 p-3 rounded-lg hover:bg-gray-50 dark:hover:bg-white/5 transition-colors">
                <div class="flex-shrink-0 w-12 h-12 flex items-center justify-center rounded-full bg-accent-dark/10 text-accent-dark dark:bg-accent-light/10 dark:text-accent-light">
                  <span class="font-bold">{landmark.distance}</span>
                </div>
                <div>
                  <h4 class="font-medium text-gray-900 dark:text-white">
                    {landmark.name}
                  </h4>
                  <p class="text-sm text-gray-600 dark:text-gray-300">
                    {landmark.description}
                  </p>
                </div>
              </div>
            ))
          }
        </div>
      </div>

      <div>
        <h3 class="text-xl font-bold mb-6 flex items-center gap-2 text-gray-darkest dark:text-white">
          <svg class="w-5 h-5 stroke-current">
            <use href="/ui.svg#map"></use>
          </svg>Cómo Llegar
        </h3>
        <div class="space-y-6">
          {
            TRANSPORT.map((transport) => (
              <div class="border-l-4 border-accent-dark pl-4 dark:border-accent-light">
                <h4 class="font-semibold text-gray-900 dark:text-white">
                  {transport.type}
                </h4>
                <p class="text-sm font-medium text-accent-dark dark:text-accent-light mt-1">
                  {transport.details}
                </p>
                <p class="text-sm text-gray-600 dark:text-gray-300 mt-1">
                  {transport.instructions}
                </p>
              </div>
            ))
          }
        </div>
      </div>
    </div>

    <div class="bg-gradient-to-r from-accent-dark to-accent-light rounded-xl p-8 mt-12">
      <div class="max-w-3xl mx-auto text-center">
        <h3 class="dark:text-white text-2xl md:text-3xl font-bold mb-4">¿Necesitas indicaciones personalizadas?</h3>
        <p class="dark:text-white/90 mb-6 text-lg">Nuestro equipo está listo para guiarte paso a paso hasta nuestras instalaciones</p>
        <div class="flex flex-col sm:flex-row gap-4 justify-center">
          <a
            href={CONTACT.whatsapp}
            target="_blank"
            rel="noopener noreferrer"
            class="inline-flex justify-center items-center px-6 py-3 rounded-lg dark:bg-white bg-yellow-400 dark:text-black text-accent-dark font-semibold hover:bg-gray-100 transition-all duration-300 gap-2"
            aria-label="Contactar por WhatsApp"
          >
            <svg class="w-5 h-5 stroke-current" aria-hidden="true">
              <use href="/ui.svg#message"></use>
            </svg>
            Contactar por WhatsApp
          </a>
          <div class="inline-flex justify-center items-center px-6 py-3 rounded-lg bg-gray-200 dark:bg-black/40 dark:backdrop-blur-md dark:border dark:border-white/30 border-white dark:text-white font-semibold hover:bg-gray-400 transition-all duration-300 gap-2">
            <svg class="w-5 h-5 stroke-current" aria-hidden="true">
              <use href="/ui.svg#phone"></use>
            </svg>
            +51 987 979 257
          </div>
        </div>
      </div>
    </div>
  </section>
</Container>
